// start

    // ask user for a number between 1 and 100
    // if number >= 1 and <= 100
        // for i = 1; i <= number; i++
            // display i
            // display break a new line
        // endfor
    // else 
        // display error message
    // endif

// Stop